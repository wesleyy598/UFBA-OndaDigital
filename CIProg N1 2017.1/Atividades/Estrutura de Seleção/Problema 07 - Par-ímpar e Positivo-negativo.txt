#include <stdio.h>

int main(){
  
  int num;
  
	scanf("%d",&num);
	if (num < 0)
	{
		printf("Número negativo\n");
	}
	else if (num >= 0)
	{
		printf("Número positivo\n");
	}
	if (num % 2 == 0)
	{
		printf("Número par\n");
	}
	else if (num % 2 != 0)
	{
		printf("Número ímpar\n");
	}
  return 0;
}